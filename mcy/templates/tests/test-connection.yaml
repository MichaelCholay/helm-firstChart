apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mcy.fullname" . }}-test-connection"
  labels:
    {{- include "mcy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mcy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
