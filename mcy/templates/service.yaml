apiVersion: v1
kind: Service
metadata:
  name: {{ include "mcy.fullname" . }}
  labels:
    {{- include "mcy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mcy.selectorLabels" . | nindent 4 }}
